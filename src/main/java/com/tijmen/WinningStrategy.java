package com.tijmen;

public class WinningStrategy implements Strategy {
    @Override
    public Actor nextMove() {
        // Calculate cumulative average of our playing lines
        // Find the minimum average at each line
        // Pick line with highest minimum
        return null;
    }
}
